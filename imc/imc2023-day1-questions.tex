\documentclass{article}
\usepackage{amsmath, amssymb, amsthm}

\title{IMC 2023, First Day}
\date{August 2, 2023}

\begin{document}
\maketitle

\section*{Problem 1}
Find all functions $f : \mathbb{R} \to \mathbb{R}$ that have a continuous second derivative and for which the equality $f(7x+1) = 49 f(x)$ holds for all $x \in \mathbb{R}$.

\section*{Problem 2}
Let $A$, $B$ and $C$ be $n \times n$ matrices with complex entries satisfying
\[ A^2 = B^2 = C^2 \quad \text{and} \quad B^3 = ABC + 2I. \]
Prove that $A^6 = I$.

\section*{Problem 3}
Find all polynomials $P$ in two variables with real coefficients satisfying the identity
\[ P(x,y) P(z,t) = P(xz - yt, xt + yz). \]

\section*{Problem 4}
Let $p$ be a prime number and let $k$ be a positive integer. Suppose that the numbers $a_i = i^k + i$ for $i = 0, 1, \ldots, p-1$ form a complete residue system modulo $p$. What is the set of possible remainders of $a_2$ upon division by $p$?

\section*{Problem 5}
Fix positive integers $n$ and $k$ such that $2 \leq k \leq n$ and a set $M$ consisting of $n$ fruits. A \emph{permutation} is a sequence $x = (x_1, \ldots, x_n)$ such that $\{x_1, \ldots, x_n\} = M$. Ivan \emph{prefers} some (at least one) of these permutations. He realized that for every preferred permutation $x$, there exist $k$ indices $i_1 < i_2 < \cdots < i_k$ with the following property: for every $1 \leq j < k$, if he swaps $x_{i_j}$ and $x_{i_{j+1}}$, he obtains another preferred permutation.

Prove that he prefers at least $k!$ permutations.

\end{document}
